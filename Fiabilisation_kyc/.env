# ─── SÉCURITÉ — NE JAMAIS COMMITTER CE FICHIER EN PRODUCTION ──────────────────
# Générer une nouvelle SECRET_KEY avec :
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

SECRET_KEY=mr#@#xsddb#zx2g418x)t8_l%whol$v6run(u84n-c^7^f2or$

# ─── MODE APPLICATION ──────────────────────────────────────────────────────────
# PRODUCTION : DEBUG=False obligatoire
DEBUG=True

# ─── HÔTES AUTORISÉS ──────────────────────────────────────────────────────────
# Lister uniquement les domaines réels (plus de 0.0.0.0 ni localhost en prod)
ALLOWED_HOSTS=kyc-test.of.africa,kyc.of.africa,10.170.82.22,127.0.0.1,localhost

# ─── HTTPS & COOKIES (production) ─────────────────────────────────────────────
# Activer le redirect HTTPS (désactiver uniquement en dev local)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0

# ─── BASE DE DONNÉES (décommenter pour MSSQL) ─────────────────────────────────
# DB_NAME=kyc_notation
# DB_HOST=10.170.83.20
# DB_PORT=1433
